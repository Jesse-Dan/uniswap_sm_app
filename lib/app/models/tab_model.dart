class Tabber {
  final bool show;
  const Tabber({required this.show});
}

List<Tabber> tabbers = [
  Tabber(show: true),
  Tabber(show: false),
  Tabber(show: false),
  Tabber(show: false),
  Tabber(show: false),
];
